# 电气与时序

## 1. 时钟与带宽

| 项 | 值 |
| --- | --- |
| SPI 主机 | `SPI2_HOST` |
| QSPI 时钟 | 80 MHz |
| 数据线数 | 4 |
| 理论上限 | 80 MHz × 4 bit = 320 Mbit/s = 40 MB/s |
| 整屏数据量 | 320 × 480 × 2 B = 307,200 B |
| 整屏传输理论下限 | 7.7 ms |
| 像素格式 | RGB565，16 bpp |

QSPI 时钟 80 MHz 高于 ST77922 数据手册规定的写时序上限 62.5 MHz，见 [`03-display-st77922.md`](03-display-st77922.md) 第 5.1 节。上表带宽上限按 80 MHz 计算。

## 2. 时间指标

实测条件：ESP-IDF 5.5.4，CPU 160 MHz，无 GUI 框架。各阶段对应的实现见 [`03-display-st77922.md`](03-display-st77922.md)。

| 阶段 | 耗时 | 说明 |
| --- | --- | --- |
| 复位 → `app_main` | 约 111 ms | 含二级 bootloader |
| 屏初始化 | 约 260 ms | 总线初始化、面板 IO、驱动创建、复位、初始化序列、反色、`DISPON`。其中固定延时合计 260 ms（软复位 120 ms + `SLPOUT` 120 ms + `TEON`（`0x35`）20 ms），占该阶段耗时的主要部分 |
| 整屏填充 | ≤ 10 ms | 15 次分带传输，每带 320 × 32 像素 |
| 文本块绘制（264 × 24） | < 1 ms | 单次传输 12,672 B |

前提：`esp_lcd_panel_draw_bitmap()` 每次调用都先发 `CASET`、`RASET`，再发 `RAMWR` 色数据；`esp_lcd_panel_io_spi` 的 `tx_param()` 每次下发命令相前都先排空全部在途事务，`tx_color()` 在带命令时同样先排空。因此每次调用都会阻塞到上一带 DMA 传输完成，返回时上一带色数据已传完、本带色数据已入队，有效流水线重叠只有 1 带。上表的绘制耗时为调用返回耗时：15 次分带调用互相排空，累计耗时约为各带传输时间之和，不含最后一带的传输完成。

确需更深流水时须避开发带命令的调用路径：在单次 `tx_color` 内传输完整区域。

`on_color_trans_done` 回调不改变流水深度，只适用于节流。该回调在 SPI 中断上下文中执行，且先于事务结果入队，不可在其中调用 `esp_lcd_panel_draw_bitmap()`：该调用内部对在途事务的排空会等待一个尚未入队的结果。

采样方式：单次运行的串口日志时间戳，分辨率 1 ms。屏初始化耗时由初始化序列内的固定延时主导，分解见本节的表格，此处不再重复。

## 3. 上电与复位

| 项 | 说明 |
| --- | --- |
| 主控上电时序 | 见 ESP32-S3 数据手册 §2.7 表 7 |
| CHIP_PU（EN）网络 | 10 kΩ 上拉 + 延时电容构成的 RC 网络。按下 RESET 使 EN 拉低，松开后经电容充电延时释放 |
| 复位不成功时的处置 | 增大延时电容容值以延长 EN 低电平时间 |
| 复位域 | EN 同时复位 ESP32-S3 与面板，两者共用同一复位信号 |

面板无独立复位 GPIO，运行中无法单独复位；面板进入异常状态时只能整机复位。

## 4. 启动配置引脚

4 个 strapping 引脚为 GPIO0、GPIO3、GPIO45、GPIO46，在 CHIP_PU 上升沿被采样。

| 参数 | 说明 | 最小值 |
| --- | --- | --- |
| t_SU | CHIP_PU 拉高前，strapping 电平须已建立 | 0 ms |
| t_HD | CHIP_PU 拉高后，strapping 电平须保持 | 3 ms |

本模块的分配与约束见 [`01-hardware.md`](01-hardware.md) 第 2.9 节。

## 5. 电流

| 工况 | 电流 |
| --- | --- |
| ESP32-S3 复位 | 0 |
| 仅显示屏工作 | 198 mA |
| 显示屏 + 喇叭 + 电池充电 | 401 mA |

上表为厂商给出的点值，背光自身电流见第 7 节。峰值电流与上电冲击电流未测量，供电端与线材的选型依据见 [`07-open-items.md`](07-open-items.md)。[文档]

## 6. 热

| 器件 | 说明 |
| --- | --- |
| 充电管理 IC（TP4054） | 表面温度上限 62 ℃（厂商给出） |
| LDO（ME6217C33M5G） | 使用中发热，勿直接接触 |
| 充电饱和电压 | 4.24 V |
| 充电电流 | 最大 500 mA；模块实际值 290 mA（厂商给出） |
| 工作 / 存储温度 | −30 ~ +80 ℃ |

本节数值均为厂商给出，未经测量。

## 7. 背光

本节数值取自厂商规格书与原理图，未实测；通断状态已实测，见 [`README.md`](README.md) 覆盖边界。[文档]

| 项 | 值 |
| --- | --- |
| 控制引脚 | IO41，高电平点亮 |
| 上电默认 | 灭 |
| 驱动方式 | N 沟道 MOSFET（BSS138）低边开关 |
| 负载 | 6 颗白光 LED 并联，正极接 3.3 V，负极经开关接地 |
| 限流电阻 | R33；减小阻值可提高最大亮度 |
| 电流 | 120 mA |
| 亮度 | 300 cd/m²（典型） |
| 调光 | IO41 可输入 PWM。厂商参考做法：LEDC，低位速度模式，10 位分辨率，5 kHz [文档] |

亮度与占空比的对应关系未标定，见 [`07-open-items.md`](07-open-items.md)。
