# 故障定位

按症状检索。根因与完整说明见所指章节。

## 索引

| 症状 | 首要怀疑 | 章节 |
| --- | --- | --- |
| 背光亮，屏幕无任何内容 | 初始化序列与本模块玻璃不匹配 | [1](#1-背光亮但无内容) |
| 背光不亮，屏幕全黑 | 背光引脚未拉高；供电异常 | [2](#2-背光不亮) |
| 显示内容颜色偏移，深色发紫或发红 | RGB565 字节序未交换 | [3](#3-颜色偏移) |
| 黑白颠倒，整体反色 | 未下发 `INVON` | [4](#4-黑白颠倒) |
| 局部刷新花屏、列错位 | 列坐标未按 4 像素对齐 | [5](#5-局部区域异常) |
| 烧录失败 | 未进入下载模式；串口被占用 | [6](#6-烧录失败) |
| 串口无日志 | 控制台未切至 USB-Serial-JTAG；权限不足 | [7](#7-串口无日志) |
| 切走页面后仍有声音或指示灯亮 | 未还原该页面改动的硬件状态 | [9](#9-音频与指示灯残留) |
| 关闭再打开编解码时出现 I2S 通道错误 | 停止输出误用了关闭流程 | [9](#9-音频与指示灯残留) |

## 1. 背光亮但无内容

**判据**：背光可点亮，串口可见 `esp_lcd_panel_init()`、`esp_lcd_panel_draw_bitmap()` 等调用均返回 `ESP_OK`，屏幕无图像。

**根因**：`espressif/esp_lcd_st77922` 内置的 `vendor_specific_init_default` 面向 532×300 面板，本模块为 320×480。该情况下面板供电正常、API 全部成功，但不产生图像。

**处置**：传入本模块的初始化序列并置 `vendor_config.flags.use_qspi_interface = 1`。序列见 [`03-display-st77922.md`](03-display-st77922.md) 第 4.2 节。

**验证方式**：`ESP_OK` 返回值不能作为上屏成立的判据。须以屏幕实际输出为准。

## 2. 背光不亮

**判据**：通电后屏幕无辉光。

**排查顺序**：

1. IO41 电平。高为点亮，低为熄灭，上电默认为灭，须在程序中置高。
2. 供电。测量 Type-C 输入电压与 3.3 V 输出。
3. 排线。FPC 是否插到位。

IO41 为普通 GPIO，无复用冲突。

## 3. 颜色偏移

**判据**：单一颜色被解析为另一颜色。典型表现：`RGB565(0x10, 0x18, 0x40)`（深蓝）呈现为品红。白色与灰度内容不受影响，因 `0xFFFF` 与对称灰阶在字节交换下不变。

**根因**：面板按高字节先接收 RGB565；ESP32-S3 小端序缓冲区按低字节先输出。详见 [`03-display-st77922.md`](03-display-st77922.md) 第 6 节。

**处置**：写入 `esp_lcd_panel_draw_bitmap()` 的全部像素须经高低字节交换。

**不可用的替代方案**：`esp_lcd_panel_dev_config_t.data_endian` 对该驱动不起作用，该字段仅由 ST7789 驱动读取。

## 4. 黑白颠倒

**判据**：预期白色处显示黑色，反之亦然。

**根因**：IPS 面板需启用显示反色，未下发 `INVON`（`0x21`）。

**处置**：确保初始化序列包含 `{0x21, ..., 0, 0}`，或在 `esp_lcd_panel_init()` 后调用 `esp_lcd_panel_invert_color(panel, true)`。二者等效且幂等。传入 `false` 会下发 `INVOFF`（`0x20`）并撤销反色。

## 5. 局部区域异常

**判据**：整屏刷新正常，局部区域刷新出现错位、撕列或空白带。

**根因**：ST77922 为双栅极驱动，像素数须为 4 的倍数。`esp_lcd_panel_draw_bitmap()` 的 `x_start` 与 `x_end` 须各自为 4 的倍数。

**处置**：调整区域边界至 4 的倍数，或扩大刷新区域。例：宽度 264 的文本块居中于 320 宽屏幕时，取 `x_start = 28`、`x_end = 292`。

## 6. 烧录失败

**判据**：esptool 无法同步，或写入中途断开。

**排查顺序**：

1. 手动进入下载模式，见 [`04-build-and-flash.md`](04-build-and-flash.md) 第 8 节。
2. 确认串口设备未被其他进程占用（`idf.py monitor`、串口终端）。
3. 确认设备节点正确。接入后核对 USB ID `303a:1001`。
4. 确认当前用户具备串口访问权限，见 [`04-build-and-flash.md`](04-build-and-flash.md) 第 7 节。

## 7. 串口无日志

**判据**：设备已运行但 `/dev/ttyACM0` 无输出。

**排查顺序**：

1. `CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y`。缺省为 UART0，日志只从 IO43/IO44 输出。
2. 串口访问权限。
3. 上一次烧录的程序占用了 UART0 或关闭了控制台输出。

## 8. 不构成判据的现象

| 现象 | 说明 |
| --- | --- |
| `esp_lcd_*` 全部返回 `ESP_OK` | 面板不可见状态下的调用同样返回成功，见第 1 节 |
| 出现 `The 3Ah command has been used and will be overwritten` 等警告 | 初始化序列覆盖驱动内置值时的预期输出，非故障 |
| 背光点亮 | 仅说明 IO41 与背光电路正常，与面板数据通路无关 |

## 9. 音频与指示灯残留

**判据一**：切走页面后扬声器仍在发声，或指示灯仍亮。

**根因**：该页面改变了硬件状态但未在离开时还原。指示灯会保持最后一次设置的颜色；编解码在打开状态下会持续输出。

**处置**：页面模型需提供进入与离开两个钩子，离开钩子还原该项改动的状态——指示灯熄灭、背光恢复满亮度、音频静音。仅靠"离开后不再调用输出函数"不足以保证静音，已写入 DMA 的数据仍会播完。

**判据二**：停止输出后再开始输出时，串口出现

```
E i2s_common: i2s_channel_disable: the channel has not been enabled yet
```

**根因**：停止输出用的是关闭编解码，而重新打开时 `esp_codec_dev` 内部会对已关闭的 I2S 通道再次调用 `i2s_channel_disable`。`esp_driver_i2s` 对未启用的通道打错误日志，该返回值被调用方忽略，功能不受影响。

**处置**：停止输出改用静音，不关闭编解码。静音写 ES8311 的 DAC 静音位，输出置零，不涉及通道状态。关闭流程会一并禁用功放使能引脚，若确实需要关闭功放，应在关闭后不再重新打开。
